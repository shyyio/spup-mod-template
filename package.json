{
    "name": "spup-mod-template",
    "version": "2.0.0",
    "private": true,
    "description": "A starting point for a Shy's Power-Up Factory mod",
    "type": "module",
    "scripts": {
        "dev": "spup-dev client",
        "build": "spup-mod-builder build",
        "check": "spup-mod-builder check",
        "test": "node --import @spup/game-server/test-loader --test test/*.spec.js",
        "dev:server": "spup-dev server"
    },
    "devDependencies": {
        "@spup/game-client": "^2.0.0",
        "@spup/game-server": "^2.0.0",
        "@spup/mod-builder": "^2.0.0",
        "@spup/sdk": "^2.0.0",
        "pixi.js": "^8.19.0"
    },
    "engines": {
        "node": ">=20"
    }
}
